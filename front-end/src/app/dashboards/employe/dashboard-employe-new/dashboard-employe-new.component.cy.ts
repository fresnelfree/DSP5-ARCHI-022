import { DashboardEmployeNewComponent } from './dashboard-employe-new.component'

describe('DashboardEmployeNewComponent', () => {
  it('should mount', () => {
    cy.mount(DashboardEmployeNewComponent)
  })
})